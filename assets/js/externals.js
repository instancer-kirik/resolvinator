const React = window.React;
const ReactDOM = window.ReactDOM;

import * as THREE from 'three';
import { Canvas, useFrame } from '@react-three/fiber';

const { useGLTF, Environment, Lightformer, OrbitControls }  = window.Drei;
export { React, ReactDOM, THREE, Canvas, useFrame, useGLTF, Environment, Lightformer, OrbitControls };


